server:
  port: 6080
spring:
  application:
    name: spring-cloud-config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/zszpzhzq/spring-cloud-demo-base.git
          search-paths: config # config开头的目录搜索配置文件多个用，隔开
