server:
  port: 6080
spring:
  application:
    name: spring-cloud-config-server
  cloud:
    config:
      server:
        git:
          uri: git@github.com:zszpzhzq/configcenter.git
          search-paths: config/service,config/eureka,config/web # config开头的目录搜索配置文件多个用，隔开

